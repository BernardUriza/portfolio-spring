# Development profile sample (do not commit secrets)

# Enable admin security in dev
portfolio.admin.security.enabled=true

# Provide admin token via environment variable instead of hardcoding here
# PORTFOLIO_ADMIN_TOKEN will be read by AdminTokenAuthenticationFilter
# You can still uncomment the line below locally but DO NOT commit it
# portfolio.admin.token=replace-with-your-dev-token

# Optional CORS for local dev (uncomment if needed)
# spring.mvc.cors.mappings.[/api/**].allowed-origins=http://localhost:4200
# spring.mvc.cors.mappings.[/api/**].allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
# spring.mvc.cors.mappings.[/api/**].allowed-headers=Authorization,Content-Type,X-Admin-Token,X-Requested-With
# spring.mvc.cors.mappings.[/api/**].allow-credentials=true
